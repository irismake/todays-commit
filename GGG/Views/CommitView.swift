import SwiftUI

struct CommitView: View {
  var body: some View {
    HStack(spacing: 12) {
      Text("test")
        .font(.headline)
        .fontWeight(.semibold)
        .foregroundColor(Color(.black))
    }
  }
}
